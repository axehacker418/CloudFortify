"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const Help = () => {
  return (
    <Card className="w-full p-4">
      <CardHeader>
        <CardTitle>Help</CardTitle>
      </CardHeader>
      <CardContent>
        {/* TODO: Implement help section */}
        <p>FAQs, video tutorials, and live support options will be available here.</p>
      </CardContent>
    </Card>
  );
};
